language: php
php:
  - 7.1
  - 7.2

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

install: composer update --prefer-lowest --no-interaction

script: phpdbg -qrr vendor/bin/phpunit --log-junit "reports/junit.xml" --coverage-clover="reports/coverage.xml"
